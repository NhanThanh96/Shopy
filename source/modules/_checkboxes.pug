//-------------------------------------------------------
//- Modules :: Checkbox
//-------------------------------------------------------
mixin group-input-checkbox({name, id, label, disabled, isValid, isInValid, isIndeterminate, desc, noticeError, iconError, checked})
  .form-group.input-checkbox-custom()&attributes(attributes)
    input.input-checkbox(
      type= 'checkbox'
      name= name ? `${name}` : null
      id= id ? `${id}` : null
      checked= checked ? 'checked' : null
      disabled= disabled ? 'disabled' : null
      class= isValid ? 'input-checkbox--valid' : null
      class= isInValid ? 'input-checkbox--invalid' : null
      class= isIndeterminate ? 'input-checkbox--indeterminate js-checkbox-indeterminate' : null
    )
    label(for= id ? `${id}` : null)
      | #{label}
    if desc
      .input-desc
        | #{desc}
    if noticeError
      .error-notice
        if iconError
          i(class= iconError)
        | #{noticeError}

    
